name: shoe-ingestor
runtime: python3.10
install: pip install --no-cache-dir -r requirements.txt
start: gunicorn --worker-tmp-dir /tmp --bind 0.0.0.0:8080 --timeout 600 --no-control-socket --workers 1 ingestor:app
env:
  PORT: 8080