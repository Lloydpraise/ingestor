name: shoe-ingestor
runtime: python3.10
install: python -m pip install --upgrade pip && python -m pip install --no-cache-dir -r requirements.txt
start: gunicorn --workers 1 --bind 0.0.0.0:8080 --timeout 600 ingestor:app
env:
  PORT: 8080